# Backend base URL (include /v1 prefix). On Android emulator use http://10.0.2.2:8080/v1
EXPO_PUBLIC_API_BASE_URL=http://localhost:8080/v1

# Supabase access token for authenticated API calls until in-app auth is wired up
EXPO_PUBLIC_ACCESS_TOKEN=
